spring:
  profiles:
    active: dev
  application:
    name: workshop
  jpa:
    open-in-view: false
server:
  servlet:
    context-path: /api

rsa:
  private-key: classpath:certifications/private_key.pem
  public-key:  classpath:certifications/public_key.pem
